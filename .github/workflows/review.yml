name: Review

# on:
#   push:
#     branches-ignore:
#       - main
#     tags-ignore:
#       - v*

concurrency:
  cancel-in-progress: true
  group: review-${{ github.ref }}

jobs:
  register:
    name: Build & Register application
    runs-on: ubuntu-latest
    steps:
      - name: Get base hostname
        run: |
          REPOSITORY=${GITHUB_REPOSITORY#*/}
          BRANCH=${GITHUB_HEAD_REF:-${GITHUB_REF#refs/heads/}}
          HOST=$(npx kube-workflow slugify $REPOSITORY-$BRANCH)
          echo "host=${HOST}" >> $GITHUB_ENV
      - name: Use autodevops build and register
        uses: SocialGouv/actions/autodevops-build-register@v1
        with:
          environment: dev
          imagePackage: app
          token: ${{ secrets.GITHUB_TOKEN }}
          dockerbuildargs: |
            NEXT_PUBLIC_HOST="${{ env.host }}.dev.fabrique.social.gouv.fr"
            NEXT_PUBLIC_APP_VERSION_COMMIT=${{ github.sha }}

  deploy:
    name: Deploy review branch
    runs-on: ubuntu-latest
    needs: [register]
    steps:
      - name: Use kube-workflow deployment
        uses: SocialGouv/kube-workflow@v1
        with:
          environment: dev
          token: ${{ secrets.GITHUB_TOKEN }}
          kubeconfig: ${{ secrets.KUBECONFIG }}
          rancherProjectId: ${{ secrets.RANCHER_PROJECT_ID }}
          rancherProjectName: ${{ secrets.RANCHER_PROJECT_NAME }}
