projectName: fabrique
ciNamespace: ci-fabrique

dependencies:
  fabrique:
    extends:
      - name: ovh
        ifEnv: [dev, preprod]
      - name: buildkit-service
        ifEnv: [dev, preprod]
    dependencies:
      contrib:
        patches:
          needsUsingArgocdSyncWaves:
            enabled: true
